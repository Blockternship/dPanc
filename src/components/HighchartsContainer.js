import React from 'react';
import ReactHighcharts from 'react-highcharts';

const HighchartsContainer = ({ config }) => (
  <ReactHighcharts config={config} />
);

export default HighchartsContainer;
